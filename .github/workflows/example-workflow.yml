name: Example workflow using our action
on:
  workflow_dispatch:

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Run Python action
        id: hello-world
        uses: karelbemelmans/my-first-python-action@main
        with:
          who: "John Connor"

      - name: Show greeting
        run: echo "Hello, ${{ steps.hello-world.outputs.greeting }}"
